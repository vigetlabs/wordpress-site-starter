{#
  Block: Logo Grid
#}

{% set allowed = [ 'core/image' ] %}
{% set inner = {
	template: attributes.template ?? [],
	allowedBlocks: allowed,
} %}
<section {{ block_attrs( block ) }}>
	<div class="alignwide">
  		{{ inner_blocks( inner ) }}
  	</div>
</section>
