{#
  Block: Text & Image
#}

{% set inner = {
	template: attributes.template ?? [],
} %}
<section {{ block_attrs( block ) }}>
	<div class="alignwide">
		{{ inner_blocks( inner ) }}
	</div>
</section>
