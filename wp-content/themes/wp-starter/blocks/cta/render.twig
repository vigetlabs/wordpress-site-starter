{#
  Block: CTA
#}

{% set template = [
  [
    'core/pattern',
    {
      'slug': 'wp-starter/cta-inner-blocks'
    }
  ]
] %}
{% set inner = { template: template } %}
<section {{ block_attrs( block ) }}>
	<div class="alignwide">
		{{ inner_blocks( inner ) }}
	</div>
</section>
