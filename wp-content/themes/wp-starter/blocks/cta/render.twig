{#
  Block: CTA
#}

{% set template = [
  [
    'core/pattern',
    {
      'slug': 'wp-starter/cta-inner-blocks'
    }
  ]
] %}
{% set inner = { template: template } %}
<section {{ block_attrs( attributes ) }}>
  {{ inner_blocks( inner ) }}
</section>
