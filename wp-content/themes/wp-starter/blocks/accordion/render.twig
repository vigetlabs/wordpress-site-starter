{#
  Block: Accordion
#}

{% set allowed = [ 'core/details' ] %}
{% set inner = {
	template: attributes.template ?? [],
	allowedBlocks: allowed
} %}
<section {{ block_attrs( block ) }}>
	<div class="alignwide">
  		{{ inner_blocks( inner ) }}
	</div>
</section>
